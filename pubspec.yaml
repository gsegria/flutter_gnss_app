name: flutter_gnss_app
description: GNSS 航跡記錄與導航 App
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  geolocator: ^10.4.0
  google_maps_flutter: ^2.3.0
  path_provider: ^2.0.15
  csv: ^5.0.0
  flutter_bloc: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/